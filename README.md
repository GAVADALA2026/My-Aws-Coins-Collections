# MyAwsCoinsCollection

MyAwsCoinsCollection is a public teaching demo created for the seminar of the `Web Technologies` course at the Università degli Studi di Salerno.

The application has no authentication flow and does not collect, retain, or validate user credentials. It opens directly to the sample coin collection.

> This GitHub Pages deployment is public and must not be used for private data or access control.

## Authentication boundary

The prior client-only demo sign-in, credential Store state, and string-based route guard were removed. Authentication has deliberately not been replaced with an invented backend or identity-provider configuration.

Before the application manages protected data, integrate a selected OpenID Connect/OAuth 2.0 provider or backend. The integration needs the provider/tenant, client ID, redirect and logout callback URLs, permitted origins, and a server-side session strategy (for example, `HttpOnly`, `Secure`, appropriately `SameSite` cookies). Keep passwords, access tokens, refresh tokens, and client secrets out of source code, browser storage, and NgRx state. Protected routes must then validate the real server session or token rather than client-provided strings.

## Live deployment

The application is published through GitHub Pages:

- https://gavadala2026.github.io/My-Aws-Coins-Collections/

The GitHub Pages workflow builds the production application with the base URL
`https://gavadala2026.github.io/My-Aws-Coins-Collections/` and publishes the
artifact generated in `dist/my-aws-coins-collection/browser`.

## Technology stack

- Angular `21.2.22`
- Angular Material `21.2.14`
- NgRx Store and Effects `21.1.0`
- RxJS `7.8.2`
- Tailwind CSS `4.2.2`
- Node.js `24.15.0` (declared in `.nvmrc`)
- Jest `30.4.2` with `jest-preset-angular`

## How to run

Set your Node environment:

```bash
nvm use
```

Install the exact dependency tree recorded in the lockfile:

```bash
npm ci
```

Start the development server:

```bash
npm run start
```

The app will be available at `http://localhost:4200/`.

## Quality and delivery report

Status: completed and verified on 2026-08-27.

### Test suite and coverage

The Jest suite covers models, actions, reducers, RxJS services, NgRx Effects,
guards, routes, pipes, Angular components, form boundaries and integrated
Store → Effects → UI flows.

| Verification | Verified result |
|---|---:|
| Jest suites | 17 PASS / 17 total |
| Jest tests | 113 PASS / 113 total |
| Statements | 100.00% (639/639) |
| Branches | 100.00% (96/96) |
| Functions | 100.00% (24/24) |
| Lines | 100.00% (639/639) |

The enforced Jest thresholds are Statements/Lines ≥ 98% and
Branches/Functions ≥ 95%. A deliberately impossible Statements threshold of
101% was tested and made Jest fail, proving that the gate is effective.

Run the complete local quality gate:

```bash
npm run lint
npx tsc --project tsconfig.spec.json --noEmit
npm test
npm run test:coverage
npm run build
```

Local coverage deliverables are regenerated by `npm run test:coverage`:

- `coverage/lcov-report/index.html`
- `coverage/lcov.info`
- `coverage/coverage-summary.json`

### Continuous integration

[GitHub Actions](.github/workflows/jest-quality-gate.yml) runs on pull
requests to `main`, pushes to `main`, and manual dispatches. It uses the
version declared in `.nvmrc`, runs `npm ci`, lint, Jest, coverage thresholds
and the Angular build, then uploads coverage as a 14-day artifact.

The quality gate on `main` was verified successfully in
[run #33071852124](https://github.com/GAVADALA2026/My-Aws-Coins-Collections/actions/runs/33071852124).

### Dependency-security remediation

Issue [#7](https://github.com/GAVADALA2026/My-Aws-Coins-Collections/issues/7)
started from an `npm audit` result of 29 findings: 1 critical, 20 high,
5 moderate and 3 low. The compatible Angular 21 patch updates and transitive
lockfile updates were reviewed and applied without `--force` or
`--legacy-peer-deps`.

Final verification after `npm ci`:

```text
npm audit: 0 vulnerabilities
```

The remediation updates the Angular framework and build toolchain to the
secure `21.2.22` patch line, refreshes vulnerable transitive packages (including
`tar`, `undici`, `vite`, `piscina`, `postcss` and `esbuild`) and keeps the
lockfile reproducible. Informational npm deprecation notices are not audit
vulnerabilities and remain outside this security finding count.

### Traceability

| Issue | Delivered scope |
|---|---|
| [#1](https://github.com/GAVADALA2026/My-Aws-Coins-Collections/issues/1) | Coin Effects hardening and tests |
| [#2](https://github.com/GAVADALA2026/My-Aws-Coins-Collections/issues/2) | `authGuard` completion and multi-emission tests |
| [#3](https://github.com/GAVADALA2026/My-Aws-Coins-Collections/issues/3) | `NewCoin` validation boundaries and country-flag regression |
| [#4](https://github.com/GAVADALA2026/My-Aws-Coins-Collections/issues/4) | Integrated Store → Effects → UI flows |
| [#5](https://github.com/GAVADALA2026/My-Aws-Coins-Collections/issues/5) | Jest coverage thresholds and closed test register |
| [#6](https://github.com/GAVADALA2026/My-Aws-Coins-Collections/issues/6) | GitHub Actions Jest quality gate and coverage artifact |
| [#7](https://github.com/GAVADALA2026/My-Aws-Coins-Collections/issues/7) | Controlled npm audit remediation and lockfile refresh |

The detailed test register is available in
[TEST_CONTROL_PLAN.md](TEST_CONTROL_PLAN.md).
